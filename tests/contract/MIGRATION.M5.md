# Contract-test migration ledger (M5)

One row per legacy test. Outcomes: `moved -> <new file>`, `split -> <new files>`,
`deleted, duplicate of <baseline id>`, `left for M2 (<rule>)`. Removed by the
final migration ticket.

| legacy file                 | describe path > title                                                                                                                           | outcome                                                                                                                                                                                                                                       |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/contract.test.ts`      | Single-document adapter contract > unsupported options > validates focus and blur options like the sibling actions                              | left for M2 (option-validation)                                                                                                                                                                                                               |
| `src/contract.test.ts`      | Single-document adapter contract > unsupported options > forwards explicit timeout through locator terminal actions                             | left for M2 (timeouts)                                                                                                                                                                                                                        |
| `src/contract.test.ts`      | Single-document adapter contract > browser-side actions > dispatches initialized events through the pinned InjectedScript                       | moved -> `tests/contract/dispatchEvent.test.ts`                                                                                                                                                                                               |
| `src/contract.test.ts`      | Single-document adapter contract > Page compatibility façade > delegates browser-feasible Page actions without recursive dispatch               | split -> `tests/contract/selectOption.test.ts` (only the `page.selectOption` assertion is in M5 scope; the rest of this test covers click/fill/press/type/focus/hover/check/uncheck/setChecked, none of which have a distinct assertion here) |
| `src/contract.test.ts`      | Single-document adapter contract > common locator queries > reports a real focus failure that races an abort                                    | moved -> `tests/contract/focus.test.ts`                                                                                                                                                                                                       |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > focuses, blurs, clears, and types sequentially                         | split -> `tests/contract/focus.test.ts`, `tests/contract/blur.test.ts` (the clear/pressSequentially portions are out of M5 scope)                                                                                                             |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > preserves strictness and does not click an already-correct control     | split -> `tests/contract/focus.test.ts` (only the trailing `locator.focus()` strict-mode assertion is in M5 scope; the check-related assertions are out of scope)                                                                             |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > selects options through InjectedScript and dispatches input and change | moved -> `tests/contract/selectOption.test.ts`                                                                                                                                                                                                |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > retries selectOption until matching options are inserted               | moved -> `tests/contract/selectOption.test.ts`                                                                                                                                                                                                |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > retries selectOption after a disabled matching option becomes enabled  | moved -> `tests/contract/selectOption.test.ts`                                                                                                                                                                                                |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > re-resolves a select after its target detaches                         | moved -> `tests/contract/selectOption.test.ts`                                                                                                                                                                                                |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > times out missing select options after the configured wait             | moved -> `tests/contract/selectOption.test.ts`                                                                                                                                                                                                |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > selects text, scrolls, and emits hover events                          | split -> `tests/contract/selectText.test.ts`, `tests/contract/scrollIntoViewIfNeeded.test.ts` (the hover portion is out of M5 scope)                                                                                                          |
| `src/contract.test.ts`      | Single-document adapter contract > browser-native locator conveniences > uses the native scrollIntoViewIfNeeded primitive when available        | moved -> `tests/contract/scrollIntoViewIfNeeded.test.ts`                                                                                                                                                                                      |
| `src/browserCompat.test.ts` | noWaitAfter > drops deprecated no-op values on every supported action path                                                                      | left for M2 (option-validation)                                                                                                                                                                                                               |
| `src/browserCompat.test.ts` | (top level) > renders styled highlights and removes only the requested overlay                                                                  | split -> `tests/contract/highlight.test.ts`, `tests/contract/hideHighlight.test.ts`                                                                                                                                                           |
| `src/inputFiles.test.ts`    | Page.setInputFiles / Locator.setInputFiles > assigns exact payload bytes and metadata through InjectedScript                                    | moved -> `tests/contract/setInputFiles.test.ts` (kept the Locator form; the Page form tests the same behaviour through both forms, so it is dropped per the Page/Locator redundancy rule)                                                     |
| `src/inputFiles.test.ts`    | Page.setInputFiles / Locator.setInputFiles > replaces multiple files, emits events on each call, and clears with []                             | moved -> `tests/contract/setInputFiles.test.ts` (Locator form kept, see above)                                                                                                                                                                |
| `src/inputFiles.test.ts`    | Page.setInputFiles / Locator.setInputFiles > retargets labels and does not require enabled or visible input                                     | moved -> `tests/contract/setInputFiles.test.ts` (Locator form kept, see above)                                                                                                                                                                |
| `src/inputFiles.test.ts`    | Page.setInputFiles / Locator.setInputFiles > waits for a late input and applies timeout                                                         | moved -> `tests/contract/setInputFiles.test.ts` (Locator form kept, see above)                                                                                                                                                                |
| `src/inputFiles.test.ts`    | Page.setInputFiles / Locator.setInputFiles > rejects paths and browser-only values without changing an existing selection                       | moved -> `tests/contract/setInputFiles.test.ts` (Locator form kept, see above)                                                                                                                                                                |
| `src/inputFiles.test.ts`    | Page.setInputFiles / Locator.setInputFiles > validates input type, multiplicity, and directory restrictions                                     | moved -> `tests/contract/setInputFiles.test.ts` (Locator form kept, see above)                                                                                                                                                                |
| `src/inputFiles.test.ts`    | (top level) > keeps Locator uploads strict while Page supports strict opt-in                                                                    | moved -> `tests/contract/setInputFiles.test.ts` (this is the differing Page assertion the rule above keeps)                                                                                                                                   |
| `src/inputFiles.test.ts`    | (top level) > preserves the upstream input/change shadow DOM event contract                                                                     | moved -> `tests/contract/setInputFiles.test.ts`                                                                                                                                                                                               |
