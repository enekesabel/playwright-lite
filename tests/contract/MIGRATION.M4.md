# Contract-test migration ledger (M4)

One row per legacy test. Outcomes: `moved -> <new file>`, `split -> <new files>`,
`deleted, duplicate of <baseline id>`, `left for M2 (<rule>)`. Removed by the
final migration ticket.

| legacy file                | describe path > title                                                                                                                                     | outcome                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/contract.test.ts`     | Single-document adapter contract > unsupported options > fills text in one input event and does not type later characters after timeout                   | split -> `tests/contract/fill.test.ts`, `tests/contract/type.test.ts`                                                                              |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > fills text inputs, textareas, and contenteditables through InjectedScript                       | moved -> `tests/contract/fill.test.ts`                                                                                                             |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > fills number inputs through the pinned needsinput path                                          | moved -> `tests/contract/fill.test.ts`                                                                                                             |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > fills, replaces, and clears email inputs without using selection APIs                           | moved -> `tests/contract/fill.test.ts`                                                                                                             |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > uses InjectedScript input-type validation and editability checks for fill                       | moved -> `tests/contract/fill.test.ts`                                                                                                             |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > press accepts the delay option                                                                  | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > releases pressed keys when an abort interrupts the press delay                                  | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > presses text, Enter, modifiers, and Space with Playwright-like key details                      | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > keeps final modifiers active for keydown and removes them before keyup                          | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > applies selection on keydown and Space activation on keyup                                      | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > does not activate a newly focused button during Space keydown or keyup                          | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > waits for keyup focus microtasks before Space activation                                        | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > applies Enter defaults only for supported controls                                              | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > exposes stable page.keyboard state to the current focused element                               | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > honors keyboard cancellation and emits beforeinput before input                                 | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > orders keyboard chords, suppresses modified text, and replaces contenteditable selection        | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > uses pinned descriptions and current focus for direct keyboard events                           | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > uses the current focus for each keyboard phase after nested microtasks                          | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > follows focus inside nested open shadow roots                                                   | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > rechecks editability after keydown microtasks                                                   | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > preserves Enter input metadata through textarea insertion                                       | moved -> `tests/contract/keyboard.test.ts`                                                                                                         |
| `src/contract.test.ts`     | Single-document adapter contract > browser-side actions > routes selector presses through the shared current-focus keyboard path                          | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/contract.test.ts`     | Single-document adapter contract > browser-native locator conveniences > focuses, blurs, clears, and types sequentially                                   | split -> `tests/contract/clear.test.ts`, `tests/contract/pressSequentially.test.ts`, `tests/contract/focus.test.ts`, `tests/contract/blur.test.ts` |
| `src/contract.test.ts`     | Single-document adapter contract > browser-native locator conveniences > types text through a strict locator with delay and an explicit zero timeout      | moved -> `tests/contract/type.test.ts`                                                                                                             |
| `src/contract.test.ts`     | Single-document adapter contract > browser-native locator conveniences > types literal spaces and arbitrary Unicode without relaxing press key validation | moved -> `tests/contract/pressSequentially.test.ts`                                                                                                |
| `src/contract.test.ts`     | Single-document adapter contract > browser-native locator conveniences > uses one deadline for pressSequentially typing without a late character          | moved -> `tests/contract/pressSequentially.test.ts`                                                                                                |
| `src/contract.test.ts`     | Single-document adapter contract > browser-native locator conveniences > does not insert after a selector press deadline expires during a keyboard phase  | moved -> `tests/contract/press.test.ts`                                                                                                            |
| `src/fillRetarget.test.ts` | (top level) > fills a %s label's control                                                                                                                  | moved -> `tests/contract/fill.test.ts`                                                                                                             |
