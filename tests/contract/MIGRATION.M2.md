# Contract-test migration ledger — M2 (cross-member rule files)

Rows for ticket M2. The coordinator merges them into
`tests/contract/MIGRATION.md`. Outcome `consolidated -> <rule file> (<table or
test title>)` names where the legacy test's cases now live, so a reviewer can
check that the consolidated tables preserved coverage.

| legacy file                       | describe path > title                                                                                                                   | outcome                                                                                                                                                                                                                   |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > forwards explicit timeout through locator terminal actions                     | consolidated -> `tests/contract/rules/timeouts.test.ts` (timeouts > forwards explicit timeout through locator terminal actions)                                                                                           |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > preserves zero as an unbounded explicit locator action timeout                 | consolidated -> `tests/contract/rules/timeouts.test.ts` (timeouts > preserves zero as an unbounded explicit locator action timeout)                                                                                       |
| `src/contract.test.ts`            | Single-document adapter contract > Page compatibility façade > applies stored default timeouts to queries and waitForFunction           | consolidated -> `tests/contract/rules/timeouts.test.ts` (timeouts > applies stored default timeouts to queries and waitForFunction)                                                                                       |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > click validates the signal option                                              | consolidated -> `tests/contract/rules/option-validation.test.ts` (%s validates the signal option — `click` case)                                                                                                          |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > rejects invalid signal values, including false and null                        | consolidated -> `tests/contract/rules/option-validation.test.ts` (%s validates the signal option — `click` case, `false`/`null` signals)                                                                                  |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > validates focus and blur options like the sibling actions                      | consolidated -> `tests/contract/rules/option-validation.test.ts` (%s validates the signal option — `focus`/`blur` cases; %s rejects options it does not support — `focus`/`blur` cases)                                   |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > rejects action options other than timeout                                      | consolidated -> `tests/contract/rules/option-validation.test.ts` (%s rejects options it does not support — `fill` case)                                                                                                   |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > ignores unsupported options whose values are undefined                         | consolidated -> `tests/contract/rules/option-validation.test.ts` (option-validation > ignores unsupported options whose values are undefined)                                                                             |
| `src/contract.test.ts`            | Single-document adapter contract > unsupported options > rejects a non-numeric press or type delay                                      | consolidated -> `tests/contract/rules/option-validation.test.ts` (option-validation > rejects a non-numeric press or type delay)                                                                                          |
| `src/contract.test.ts`            | Single-document adapter contract > browser-native locator conveniences > rejects invalid or unsupported action options                  | consolidated -> `tests/contract/rules/option-validation.test.ts` (%s validates the signal option — `check` case; %s rejects options it does not support — `pressSequentially` case)                                       |
| `src/contract.test.ts`            | Single-document adapter contract > common locator queries > uses Page first-match semantics unless strict and Locator strictness always | consolidated -> `tests/contract/rules/strictness.test.ts` (strictness > uses Page first-match semantics unless strict for queries)                                                                                        |
| `src/contract.test.ts`            | Single-document adapter contract > browser-native locator conveniences > keeps Locator.type strict                                      | consolidated -> `tests/contract/rules/strictness.test.ts` (strictness > type > keeps the Locator form strict)                                                                                                             |
| `src/contract.test.ts`            | Single-document adapter contract > browser-native locator conveniences > keeps locator waitFor strict                                   | consolidated -> `tests/contract/rules/strictness.test.ts` (strictness > keeps locator waitFor strict)                                                                                                                     |
| `src/compatibilityPolish.test.ts` | Page.$name selector matching [each] > uses the first match with options %j                                                              | consolidated -> `tests/contract/rules/strictness.test.ts` ($apiName > uses the first match with Page options %j — `fill`/`focus`/`press`/`type`/`selectOption` cases)                                                     |
| `src/compatibilityPolish.test.ts` | Page.$name selector matching [each] > supports strict opt-in without side effects                                                       | consolidated -> `tests/contract/rules/strictness.test.ts` ($apiName > supports strict opt-in without side effects — same five cases)                                                                                      |
| `src/compatibilityPolish.test.ts` | Page.$name selector matching [each] > keeps the Locator form strict                                                                     | consolidated -> `tests/contract/rules/strictness.test.ts` ($apiName > keeps the Locator form strict — same five cases)                                                                                                    |
| `src/compatibilityPolish.test.ts` | Page.fill waits on the first match instead of choosing a later actionable match                                                         | consolidated -> `tests/contract/rules/strictness.test.ts` (Page.$apiName waits on the first match instead of choosing a later actionable match — `fill` case)                                                             |
| `src/compatibilityPolish.test.ts` | Page.selectOption waits on the first match instead of choosing a later actionable match                                                 | consolidated -> `tests/contract/rules/strictness.test.ts` (Page.$apiName waits on the first match instead of choosing a later actionable match — `selectOption` case)                                                     |
| `src/compatibilityPolish.test.ts` | %s.click options [each] > validates new options and ignores undefined unsupported options                                               | consolidated -> `tests/contract/rules/option-validation.test.ts` (%s.click options > validates new options and ignores undefined unsupported options)                                                                     |
| `src/pointerActions.test.ts`      | pointer action compatibility > resolves Page selectors non-strictly but keeps Locators strict                                           | consolidated -> `tests/contract/rules/strictness.test.ts` (click > uses the first match with Page options %j; click > supports strict opt-in without side effects; click > keeps the Locator form strict)                 |
| `src/pointerActions.test.ts`      | pointer action compatibility > normalizes boxed pointer options and rejects fractional click counts                                     | consolidated -> `tests/contract/rules/option-validation.test.ts` (option-validation > normalizes boxed pointer options and rejects fractional click counts)                                                               |
| `src/browserCompat.test.ts`       | noWaitAfter > accepts booleans and boxed booleans for click and press                                                                   | consolidated -> `tests/contract/rules/option-validation.test.ts` (noWaitAfter > accepts booleans and boxed booleans for click and press)                                                                                  |
| `src/browserCompat.test.ts`       | noWaitAfter > validates click and press before dispatch                                                                                 | consolidated -> `tests/contract/rules/option-validation.test.ts` (noWaitAfter > validates click and press before dispatch; %s rejects options it does not support — `click` case for the trailing `unexpected` assertion) |
| `src/browserCompat.test.ts`       | noWaitAfter > drops deprecated no-op values on every supported action path                                                              | consolidated -> `tests/contract/rules/option-validation.test.ts` (noWaitAfter > drops deprecated no-op values on every supported action path)                                                                             |
| `src/evaluation.test.ts`          | serializes DOM returns for every supported target evaluation entry point                                                                | consolidated -> `tests/contract/rules/serialization.test.ts` (serialization > serializes DOM returns for every supported target evaluation entry point)                                                                   |
